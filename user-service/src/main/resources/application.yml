server:
  port: 8082

spring:
  application:
    name: user-service
  cloud:
    nacos:
      username: nacos
      password: 123456
      config:
        enabled: false
      discovery:
        server-addr: 127.0.0.1:8848

springdoc:
  api-docs:
    group: user-service
  swagger-ui:
    path: /swagger-ui.html
