server:
  port: 9001

spring:
  application:
    name: auth-service
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
  cloud:
    nacos:
      username: nacos
      password: 123456
      config:
        enabled: false
      discovery:
        server-addr: 127.0.0.1:8848

springdoc:
  api-docs:
    group: auth-service
  swagger-ui:
    path: /swagger-ui.html
